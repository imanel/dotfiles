- defaults:
    link:
      relink: true

- clean:
  - '~'

- link:
    ~/.asdfrc:
    ~/.curlrc:
    ~/.gemrc:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.irbrc:
    ~/.railsrc:
    ~/.SpaceVim.d: spacevim
    ~/.tmux.conf:
    ~/.tool-versions:
    ~/.wgetrc:
    ~/.zshrc:
    ~/.zsh:

# - brewfile:
#     file: ../Brewfile

- shell:
  - chsh -s /usr/local/bin/zsh
