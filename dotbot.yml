- defaults:
    link:
      relink: true

- clean:
  - '~'

- link:
    ~/.asdfrc:
    ~/.curlrc:
    ~/.gemrc:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.gnupg/gpg-agent.conf: gpg-agent.conf
    ~/.irbrc:
    ~/.railsrc:
    ~/.tool-versions:
    ~/.wgetrc:
    ~/.zshrc:
    ~/.zsh:

# - brewfile:
#     file: ../Brewfile
