- defaults:
    link:
      relink: true

- clean:
  - '~'

- link:
    ~/.curlrc:
    ~/.gemrc:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.irbrc:
    ~/.railsrc:
    ~/.ruby-version:
    ~/.wgetrc:
    ~/.zshrc:

- brewfile:
    file: ../Brewfile

- shell:
  - chsh -s /usr/local/bin/zsh
