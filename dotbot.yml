- defaults:
    link:
      relink: true

- clean:
  - '~'

- link:
    ~/.curlrc:
    ~/.gemrc:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.irbrc:
    ~/.railsrc:
    ~/.ruby-version:
    ~/.wgetrc:
    ~/.zshrc:
