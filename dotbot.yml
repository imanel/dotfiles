- defaults:
    link:
      relink: true

- clean:
  - '~'

- link:
    ~/.asdfrc:
    ~/.curlrc:
    ~/.gemrc:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.gnupg/gpg-agent.conf: gpg-agent.conf
    ~/.irbrc:
    ~/.railsrc:
    ~/.SpaceVim.d: spacevim
    ~/.tmux.conf:
    ~/.tool-versions:
    ~/.wgetrc:
    ~/.zshrc:
    ~/.zsh:

# - brewfile:
#     file: ../Brewfile

# - shell:
#   - chsh -s /usr/local/bin/zsh
